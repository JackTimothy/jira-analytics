# Local deployment configuration — never commit real project definitions
projects.yaml
.env

# Build output. These are anchored to the repository root: an unanchored
# "server" would also match the cmd/server package directory.
/bin/
/dist/
/server

# Node
node_modules/
web/dist/
web/tsconfig.tsbuildinfo
*.tsbuildinfo
